export const getAllQnAs = async (req, res) => {
  res.json({ route: req.originalUrl });
};

export const addQnA = async (req, res) => {
  res.json({ route: req.originalUrl });
};

export const updateQnA = async (req, res) => {
  res.json({ route: req.originalUrl });
};
